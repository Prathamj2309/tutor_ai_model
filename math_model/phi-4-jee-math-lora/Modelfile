# 1. The Base Model
FROM phi4-mini

# 2. The LoRA Adapter (Point this to your extracted folder)
# If the Modelfile is right next to the folder, ./ works perfectly.
ADAPTER "C:\Users\Asus\tutor_ai_model\math_model\phi-4-jee-math-lora\adapter_config.json"

# 3. The ChatML Template
# This is critical so the model knows when to start and stop its <think> process.
TEMPLATE """{{ if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{ if .Prompt }}<|im_start|>user
{{ .Prompt }}<|im_end|>
{{ end }}<|im_start|>assistant
"""

# 4. Stop Words (Prevents the model from hallucinating infinite text)
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"

# 5. Math-Specific Hyperparameters
PARAMETER temperature 0.1
PARAMETER num_predict 2000